"""morningreport — the local half of the Morning Report module.

Handles the two things a static page cannot: model API calls with a
secret key, and identified data. Reads and writes the same data folder
the browser tools use.
"""

__version__ = "0.1.0"
