# Versioning Strategy

Current strategy: trunk-based development with semantic tags for milestones.

- `main` is always releasable.
- Tag notable releases as `vX.Y.Z`.
- Use patch releases for urgent fixes.
